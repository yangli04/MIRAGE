% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_inference_with_empirical.R
\name{estimate_inference_with_empirical}
\alias{estimate_inference_with_empirical}
\title{Estimate constant on-target mutation rates}
\usage{
estimate_inference_with_empirical(
  chr.info,
  treatment_fixed_count,
  control_fixed_count,
  treatment_depth,
  control_depth,
  delta = 0.001,
  depth.cutoff = 10,
  homo.cutoff = 0.99,
  lambda1 = "auto",
  top.sites = NULL,
  bg.method = "fisher",
  bg.target = "treatment",
  highly.methyl.cutoff = 0.95,
  seed = NULL,
  thread = 1
)
}
\arguments{
\item{chr.info}{A \code{data.frame} with three columns: site position in the format "chrom_coordinate_strand" (e.g., chr1_14404_-), site motif sequence (e.g., AAACC), and site motif type (e.g., NNUNN).}

\item{treatment_fixed_count}{A \code{vector} of non-mutation counts for each site in the treatment sample.}

\item{control_fixed_count}{A \code{vector} of non-mutation counts for each site in the control sample.}

\item{treatment_depth}{A \code{vector} of read coverage for each site in the treatment sample.}

\item{control_depth}{A \code{vector} of read coverage for each site in the control sample.}

\item{delta}{The sequencing error rate. Default is 0.001.}

\item{depth.cutoff}{The minimum read coverage for each site in the treatment and control sample. Default is 10.}

\item{homo.cutoff}{The minimum ratio of non-mutation counts for each site in the control sample that is used to determine homozygous sites. Default is 0.99.}

\item{lambda1}{The method to estimate the on-target mutation rate. One of "auto", "site" or a numeric value between 0 and 1.
If "auto", the on-target mutation rate is estimated from the upper quantile of candidate signal sites, where the quantile is defined by \code{highly.methyl.cutoff}.
If "site", the on-target mutation rate is estimated based on a user-defined site set that is provided by the argument \code{top.sites}.
If a numeric value between 0 and 1, the on-target mutation rate is explicitly defined as the value. Default is "auto".}

\item{top.sites}{A site set used to estimate the on-target rate. One line or vector element should correspond to one site ID. The argument is only used if \code{lambda1 = "site"}.}

\item{bg.method}{The statistical test to infer the background sites. One of "binomial", "fisher", "lrt". Default is "fisher".}

\item{bg.target}{A \code{string} indicating what type of read count data used to estimate the background mutation rate. One of "treatment", "control" or "both".
If "treatment", uses the read count data from the treatment sample; if "control", from the control sample; if "both", from both. Default is "treatment".}

\item{highly.methyl.cutoff}{The upper quantile cutoff of candidate signal sites used for estimating the on-target mutation rate. This argument name is retained for backward compatibility. Default is 0.95.}

\item{seed}{A seed for the random number generators.}

\item{thread}{A positive integer numeric specifying the number of cores for parallel computing. Default is 1.}
}
\value{
A \code{list} including a \code{data.frame} of homozygous sites, a \code{data.frame} of heterozygous sites, the estimated on-target mutation rate (lambda1), and the estimated background mutation rate (lambda2).
}
\description{
\code{estimate_inference_with_empirical} assumes a constant on-target mutation or conversion rate for all sites regardless of sequence context, and estimates site-level signal using a maximum-likelihood approach.
}
\details{
\code{estimate_inference_with_empirical} takes a site-level count table as input. The table can be generated by any preprocessing pipeline as long as it contains site IDs, motif annotations, treatment and control fixed-base counts, and treatment and control read depths in the expected order.
If one would like to estimate the on-target mutation rate using a user-defined site set, an extra site set is required. See the argument \code{top.sites} for details.
}
\examples{
# Estimate signal levels from a PAR-CLIP count table.
count_table <- read.delim(system.file("extdata", "pos.read.count.example.parclip.txt",
                                      package = "MIRAGE"))
res <- with(count_table, estimate_inference_with_empirical(cbind(pos, motif, type),
            treated_fixed_count, control_fixed_count, treated_depth, control_depth,
            delta = 0.001, depth.cutoff = 10, lambda1 = "auto", top.sites = NULL,
            bg.method = "lrt", bg.target = "both", highly.methyl.cutoff = 0.95,
            seed = 123, thread = 1))
head(res$homosites[, c("pos", "motif", "beta_est", "lrt_fdr")])

}
